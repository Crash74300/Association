{% extends 'base.html.twig' %}

{% block title %}Création d'une association{% endblock %}
{% block nav %}
    <a class="btn btn-sm btn-info m-2" href="{{ path('app_admin_association') }}" >Retour</a>
{% endblock %}

{% block content %}
    <div class="text-center mb-5">

        <h2 class="text-center m-3">Gestion des associations</h2>
    </div>
    {{ form_start(form) }}
    <div class="row justify-content-center ">
        <div class="col-10 col-md-5">{{ form_row(form.name) }}</div>
        <div class="col-10 col-md-5">{{ form_row(form.email) }}</div>
        <div class="col-10 col-md-5">{{ form_row(form.phone) }}</div>
        <div class="text-center">{{ form_row(form.submit) }}</div>

    </div>
    {{ form_end(form) }}
{% endblock %}
